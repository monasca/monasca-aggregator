sudo: false
dist: trusty
language: go
addons:
  apt:
    packages:
    - librdkafka-dev
before_install:
- curl https://glide.sh/get | sh
install:
- glide install
- go get -u github.com/golang/lint/golint
script:
- golint --set_exit_status
